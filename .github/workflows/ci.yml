name: CI

on:
  push:
    branches: 
      - develop
      - feature/**

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: install
        run: npm ci

      - name: lint
        run: npm run lint
