name: CI

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: install
        run: npm ci

      - name: lint
        run: npm run lint

      - name: build
        run: npm run build
